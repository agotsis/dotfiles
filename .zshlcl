DOTPATH=".dotconfig"
function config {
  /usr/bin/git --git-dir=$HOME/$DOTPATH/ --work-tree=$HOME $@
}

# spacex-ssh
if [ ! -z "$PS1" ] ; then
   source $(which setup-spacex-ssh)
fi

if [ -x "$(command -v fortune)" ] && [ -x "$(command -v cowsay)" ] ; then
  fortune | cowsay
fi

